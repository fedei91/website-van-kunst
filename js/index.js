"use strict";

/* possible fix for erasing the labels

 const labels = document.querySelectorAll('.lb-number');

    labels.forEach(
        label => {
            label.remove();
        }
    )

*/    